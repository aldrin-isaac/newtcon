{
  "name": "newtcon-web",
  "version": "0.0.1",
  "private": true,
  "description": "newtcon browser frontend — vanilla HTML + TypeScript-as-typed-ES-modules",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json && node scripts/copy-static.js",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/services/services.test.js test/api/newtcon/services.test.js test/api/newtcon/nodes.test.js test/api/newtcon/auth.test.js test/render-error.test.js",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
